<div class="render_user">
<h1 style=" text-align: left; padding-left: 0px; font-weight: bolder; font-size: 22px; font-weight: 400; letter-spacing: -1px;">Construye con TUx</h1>

<%= form_for(contacto) do |f| %>
  <% if contacto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contacto.errors.count, "error") %> prohibited this contacto from being saved:</h2>

      <ul>
      <% contacto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :email, placeholder: "Email" %>
  <%= f.hidden_field :tipo, value: "Usuario" %>
  <%# f.text_field :comentario %>
  <%= f.submit "Pídenos presupuestos", class: "btn btn-primary", style: "background-color: #008489; color: white; font-weight: 700;" %>
  
<% end %>
</div>