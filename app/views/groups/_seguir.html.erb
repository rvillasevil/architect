<%= form_for(@group, url: '/seguir', method: :post) do |f| %>
	<%= f.hidden_field :user_id, :value => current_user.id %>
	<%= f.hidden_field :plaza_id, :value => @plaza.id %>

<%= f.submit "Unite al círculo para estar actualizado", class: "btn btn-primary" %>
<% end %>

<%# link_to("Seguir", '/seguir', method: :post, user_id: current_user.id) do |f| %>
	<%# f.hidden_field :user_id => current_user.id %>
<%# end %>

