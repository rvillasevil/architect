<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Dejar de seguir", class: "btn", style: "border: 1px solid #d9534f; background-color: white; color: #d9534f;" %>
<% end %>